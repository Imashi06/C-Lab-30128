#include<stdio.h>
int main()
{
    int no,originalNum , rem , counter=0,result=o;
    printf("Enter any number");
    scanf("%d",&no);

    originalNum = no;

    while(originalNum!=0)
    {
        rem =originalNum % 10;
        counter++;

        originalNum /-10;
    }
}
